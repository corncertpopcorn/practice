package song;

/**
 구상
 0. 준비물
 	0-1. String - input (입력받은 내용)
 	0-2. String[] - inputArray (입력받을 내용(배열))
 	0-3. boolean[] - start(우선순위 시작, input.length), stop(우선순위 끝, input.length) / minus(음수, input.length)
 	0-4. double[] - add(양수, input.length), remove(음수, input.length)
 	0-5. int - partition(나눌 기준점, 0), sum(초기화된 신규 배열들의 항 번호, 0)
 1. 받아서 
 	1-1. input에 입력받아서 
 2. 분배해서
 3. 넣어서
 4. 계산 	
 **/

public class CalculatorFive {
	public static void cal() {
		String test = "asdasa";
		String testTwo[] = test.toCharArray();
	}
}
